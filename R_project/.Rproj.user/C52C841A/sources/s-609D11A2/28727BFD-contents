##rda_files

setwd("..")
setwd("..")
setwd("..")
setwd("2022-topic-04-team-02")
setwd("sessions")
setwd("rda_files")
save.image(file="workspace.rda")



##rds_files
#Since you are using save(), you should be using the .rda file extension instead of .rds.
#There are two main file types for saving data in R:
#.rda for saving multiple objects into a single file. Use save() and load() to work with these.
#.rds for saving a single object. Use saveRDS() and readRDS() to work with these.